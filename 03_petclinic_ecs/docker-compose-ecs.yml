version: "3"
services:
  fluentd:
    build: "./resources/fluentd"
    image: ${WEBAP_REPOSITORY}nrlabs-configurations/03_petclinic_ecs_fluentd
    container_name: fluentd
    ports:
      - "24224:24224"
    volumes:
      - ./logs/webap:/logs/webap
    logging:
      driver: fluentd
      options:
        fluentd-address: "localhost:24224"
        tag: "docker.${NR_APP_NAME}.{{.Name}}.{{.ID}}"
        fluentd-async-connect: "true"

  mysql:
    image: mysql:5.7
    container_name: mysql
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: db
      MYSQL_USER: master
      MYSQL_PASSWORD: password
      TZ: 'Asia/Tokyo'
    ports:
      - 3306:3306
    #ECS doesn't support 'depends_on', needs to use 'links' altough it is not recommended
    links:
      - fluentd
    logging:
      driver: fluentd
      options:
        fluentd-address: "localhost:24224"
        tag: "docker.${NR_APP_NAME}.{{.Name}}.{{.ID}}"
        fluentd-async-connect: "true"        

  webap:
    build: 
      context: "./resources/webap"
      args:
        - NR_APP_NAME=${NR_APP_NAME}
        - NR_LICENSEKEY=${NR_LICENSEKEY}
    image: ${WEBAP_REPOSITORY}nrlabs-configurations/03_petclinic_ecs_webap
    container_name: webap
    environment:
      DBURL: jdbc:mysql://mysql/db
      DBUSER: master
      DBPASS: password
    ports:
      - 80:8080
    #ECS doesn't support 'depends_on', needs to use 'links' altough it is not recommended
    links:
      - mysql
      - fluentd
    volumes:
      - ./logs/webap:/logs
    logging:
      driver: fluentd
      options:
        fluentd-address: "localhost:24224"
        tag: "docker.${NR_APP_NAME}.{{.Name}}.{{.ID}}"
        fluentd-async-connect: "true"        
